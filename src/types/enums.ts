enum Sizes {
    Small,
    Medium,
    Large
};

let mySize: Sizes = Sizes.Small;

let hisSize: Sizes = Sizes.Medium;


const enum Heights {
    Low,
    Medium,
    High
};

let myHeight: Heights  = Heights.High;

console.log(myHeight);